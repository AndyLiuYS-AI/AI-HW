# Design and Implementation of a Tool for Detecting AI-Generated Images

## Environment Setup
You can install the required packages by running the command:
```bash
pip install -r requirements.txt
```

## Testing
You can run the following command to test our tool:
```bash
streamlit run test2.py
```

Then you can upload images locally and check whether they are AI-generated.